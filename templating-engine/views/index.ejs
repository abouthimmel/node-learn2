
    <h1>Ini adalah halaman index/main</h1>
    <h2>halo <%= nama %></h2>
    <h4>Daftar mahasiswa</h4>
    <% if (mahasiswa.length < 1) { %>
        <p>Data mahasiswa masih kosong</p>
    <% } else %>
    <% mahasiswa.forEach(mhs => { %>
     <ul>
        <li>Nama : <%= mhs.nama %></li>
        <li>Email : <%= mhs.email %></li>
     </ul>
    <% }) %>